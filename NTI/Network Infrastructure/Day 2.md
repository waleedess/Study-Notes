- ISPs do not NAT as it has Security flaws unless they have to like in a very populated area to reduce IPs
- WE's Public IP is roughly 70 L.E monthly
- IANA -> IPs, MACs and Ports
  ICANN -> Domains
  IETF -> HTTP, DNS and TCP
  IEEE 
  W3C -> HTML and CSS